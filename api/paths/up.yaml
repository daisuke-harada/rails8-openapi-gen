get:
  tags: ["health"]
  operationId: up
  summary: "Health check"
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            type: object
            example: {}
